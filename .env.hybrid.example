# Non-secret M11 hybrid development inputs. Do not add credentials or secret values.
# `pnpm hybrid:run -- <service>` resolves all resource identifiers from stack outputs
# and assumes the dedicated development role without printing temporary credentials.
APP_ENV=hybrid
AWS_REGION=us-east-1
AWS_ACCOUNT_ID=204284492447
AWS_STACK_NAME=PirhHybridFisayoRm
ALLOW_REMOTE_AWS=true
